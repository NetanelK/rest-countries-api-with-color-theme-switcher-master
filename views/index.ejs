<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <script src="https://kit.fontawesome.com/ab464933e7.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="./style/index.css">
</head>
<body>
    <%- include('./partials/_header') %> 

    <div class="container">
        <input type="text" name="filter-text" id="filter-text" placeholder="Search for a country...">
        <select name="filter-region" id="filter-region">
            <option value=" " disabled selected hidden>Filter by Region</option>
            <div class="custom-options">
                <span class="custom-option" data-value="test">Test</span>
                <option value="africa">Africa</option>
                <option value="america">America</option>
                <option value="asia">Asia</option>
                <option value="europe">Europe</option>
                <option value="oceania">Oceania</option>
            </div>
        </select>

        <div class="cards-container">
            <% countries.forEach(country =>{ %>
                
                    <div class="card">
                        <a href="/<%= country.alpha3Code %>">
                            <img src="<%= country.flag %>" alt="">
                            <div class="details">
                                <h3><%= country.name %></h3>
                                <span><strong>Population: </strong><%= country.population.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",") %></span>
                                <span><strong>Region: </strong><span class="region" style="display: inline;"><%= country.region %></span></span>
                                <span><strong>Capital: </strong><%= country.capital %></span>
                            </div>
                        </a>
                    </div>
            <% }) %>
        </div>
    </div>
    <script src=".\scripts\theme.js"></script>
    <script src=".\scripts\index.js"></script>
</body>
</html>